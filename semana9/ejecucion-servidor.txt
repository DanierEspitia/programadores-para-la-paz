Al ejecutar el comando node server.js, Node.js inicia la aplicación y pone el servidor en funcionamiento.

En la terminal aparece el mensaje:

Servidor ejecutándose en puerto 3000

La ruta principal / responde con el mensaje:

Servidor activo y funcionando

La ruta /estado responde en formato JSON:

{
  "estado": "Servidor funcionando",
  "servicio": "API comunitaria"
}

Esto indica que el servidor está configurado correctamente y puede responder solicitudes.